# План на завтра (2026-07-14)

Контекст: `PRODUCT_PLAN.md` — общая стратегия, этот файл — конкретный
список дел на следующую сессию, по приоритету.

## 1. Сразу, без раздумий (~15 минут суммарно)

- [x] **Прогнать отложенный promptfoo-тест** (Фаза B, правило "X/Y"-нумерации
  вариантов). ✅ 2026-07-14: PASS — оба item'а ("вариант №5" и
  "вариант №5/1") корректно вернулись под variant_number=5, один с
  version_label=null (оригинал), другой с изданием. Правило работает.

- [x] **Почистить временную диагностику** в `main.py`. ✅ 2026-07-14:
  `DEBUG_GROUP_BOUNDS`/`DEBUG_DISCOVER_ENTRIES` убраны, задеплоено.

## 2. Нужно твоё решение (не техническая блокировка)

- [x] **beschwerde вариант 6, издание 2** ("Cavone GmbH"). ✅ 2026-07-14:
  выбран вариант "честно сказать, что ответа нет" — не потребовалось
  трогать схему (`response_schemas.py` уже была достаточно гибкой: у
  answer нет ограничения типа, у options нет фиксированного count).
  Правило в промпте (sentinel "(nicht angegeben)"), клиент пропускает
  его в валидации, UI показывает нейтральное сообщение вместо вопроса
  и не блокирует прохождение упражнения (_answerableQuestions). Оба
  репозитория задеплоены.

## 3. Следующий большой этап — Фаза 2 (для НЕзнакомых PDF)

Сейчас все фиксы (line-span, sentinel, HEAVY_SECTION_TYPES, спенд-кэпы)
работают либо только для флагманского документа (курируемый кэш), либо
частично для новых PDF (спенд-кэпы — да, детерминизация контента — нет).
Следующий шаг — перенести то, что уже работает вручную, в сам рантайм:

- [x] **2.1 Line-span extraction** для telefonnotiz's `weitere_informationen`.
  ✅ 2026-07-14: вместо перепечатки буллетов модель возвращает
  `{start_line, end_line}`-указатель в пронумерованный markdown, код сам
  вырезает текст (`line_extraction.py`). Плюс обнаружен и обработан
  отдельный случай — один ответ-блок, общий для нескольких изданий
  (значения через "/") — добавлено поле `slash_index`. Проверено на
  реальных данных через живые вызовы Gemini. lesen_teil2/hoeren_teil4 —
  ещё не перенесены, тот же паттерн можно повторить при необходимости.
- [ ] **2.2 Regex-извлечение ключей ответов** по маркеру "– 100%"/"(100%)"
  без LLM — не делали, не понадобилось (см. следующий пункт).
- [x] Перенести детекцию PDF-разметки (жёлтая подсветка) в сам
  `/api/convert`. ✅ 2026-07-14: `answer_markers.py`, дописывает маркер
  "– 100%" к найденным опциям автоматически для ВСЕХ типов секций
  (закрывает ~8%-й wrong-answer-key gap у beschwerde/lesen_teil4).
  По пути обнаружили и решили блокер размера Vercel-функции — PyMuPDF
  (64MB) поверх markitdown[pdf] (203MB) превышал лимит; решили не
  разделением на функции (Vercel zero-config Flask не даёт так сделать
  без рефакторинга роутинга — проверено spike'ом), а удалением самого
  markitdown: его PDF-конвертация — это буквально один вызов
  `pdfminer.high_level.extract_text()`, всё остальное (magika+onnxruntime,
  ~126MB) — угадывание типа файла, который и так уже известен. Задеплоено
  в prod, итоговый размер функции упал с ~83MB до ~52MB.

## 4. Фаза 3 — сверка как автоматический гейт

Все инструменты уже существуют и проверены (`check_answer_keys.py`,
`verify_content.py`, `check_verbatim_content.py`, `update_curated_content.py`).
✅ 2026-07-14: обвязка готова — `curation_checklist.py` прогоняет все
четыре инструмента по diff-based review scope в одном отчёте (ничего не
патчит автоматически, только отчёт для человека). `verify_content.py`
дополнительно получил `--runs N` (multi-run consensus) — один прогон LLM
audit оказался ненадёжен (подтверждено экспериментально: два прогона на
идентичном входе дали разные результаты). Осталось: пока не встроено как
обязательный гейт в CI/деплой — вызывается вручную.

## Справочно: что уже готово и задеплоено (не переделывать)

- Спенд-кэпы (Фаза 0) — полностью
- Курируемый кэш флагманского документа (doc-ключ + 12 free + 3 общих
  group-ключа) — 21 ключ ответа, обрезанные буллеты, галлюцинации
  hoeren_teil4 вариантов 7 и 8
- Правило "X/Y"-нумерации в промпте (код готов, ждёт верификации — см. п.1)
- Фикс гонки при удалении курса, fail-fast на 4xx, брендированный сплэш
  (все три — в `exam_trainer`, задеплоены)

Полный отчёт по состоянию на 2026-07-13 — в истории чата, при
необходимости попроси пересобрать.
