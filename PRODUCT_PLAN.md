# Exam Trainer: описание программы, архитектура и актуальный план

> **Единственный актуальный рабочий план и набор проектных инструкций.**
> Обновлено 2026-07-19. Устаревшие сессионные планы и завершённые отчёты
> удалены; их история остаётся в Git и локальной Hermes memory, а не
> конкурирует с этим файлом.

## Текущее состояние и правила работы

- Проект состоит из Flutter-клиента `/home/igor/project/exam_trainer` и
  Flask/Vercel backend `/home/igor/project/exam-trainer-api`; CodeGraph
  инициализирован в обоих репозиториях.
- Production API: `https://exam-trainer-api.vercel.app`. Клиент по умолчанию
  использует production; `10.0.2.2:3000` допустим только как явный
  `--dart-define=API_BASE_URL=...` для Android-эмулятора.
- Production использует discovery `v30`, parse `v37`; в коде подготовлен, но
  ещё не развёрнут dual-format rollout parse `v38`; исходный baseline
  `1.0.0+10`. APK/AAB
  `/home/igor/Downloads/exam-trainer-v41-release.*` являются архивными
  pre-P0-артефактами и **не должны загружаться в Play Console**. Они были
  собраны до исправлений CR-01—CR-06 (versionCode 10,
  targetSdk 36, SHA-256
  `b668a785bf2badce20c96a5df74157e35d37a437ba7414226fb4614f6e7fc1b8`);
  их подпись проверена, но функционально они устарели. Перед следующей
  публикацией нужно увеличить versionCode выше 10, собрать новый production
  AAB из актуального исходного кода и повторить package/API/signature/SHA-256
  gates. Архивные APK/AAB содержат выбранный тёплый редизайн,
  нативный светлый splash, startup-overlay до первого Flutter-кадра и единый
  Flutter-preloader на время Firebase, initial device gate и загрузки Home;
  единый Flutter overlay остаётся поверх MaterialApp/router/Home до кадра
  готового экрана, а нативный overlay плавно снимается за 180 мс поверх уже
  нарисованного Flutter-кадра. Промежуточные белый/чёрный кадры и отдельный
  круговой spinner удалены. В сборке только production API URL; emulator URL
  отсутствует.
  APK SHA-256:
  `48c8c29bd7c40ebcb74d259097df08b33627d83d6547e6868bc9101b892ed5cd`.
- Free tier не запускает discovery для нового PDF. Уже обработанный документ
  читается из общего doc-кэша и на клиенте безопасно сокращается до первого
  варианта каждого раздела и первой редакции telefonnotiz.
- Перед изменением prompt/schema/line-span: оценить влияние через CodeGraph,
  добавить точечную регрессию, запустить локальные тесты, затем полный
  promptfoo gate. Платный live discover/Gemini-прогон выполнять только после
  явного согласования с пользователем.
- Изменение результата parse требует bump parse-cache; изменение discovery —
  bump discover-cache. После bump также мигрировать курируемый doc-кэш и
  пересобрать подписанный APK.
- Перед production-деплоем Vercel всегда отдельно подтвердить действие с
  пользователем. Не выводить и не сохранять Firebase-токены, Gemini-ключи,
  OAuth-коды или signing passwords.
- Android production использует flavor по умолчанию `production` и прежний
  package `com.linguaproapps.exam_trainer`. Device integration smoke собирается
  только как изолированный flavor/package
  `com.linguaproapps.exam_trainer.integration`, работает на локальных fake
  зависимостях и не требует отдельного Firebase-конфига. На телефоне, где
  установлено production-приложение, запускать только
  `/home/igor/project/exam_trainer/tool/run_android_integration.sh <device-id>`.
  Скрипт передаёт Flutter `--no-uninstall`, после теста удаляет строго
  integration package и проверяет сохранность production package. Прямой
  `flutter test -d <device> integration_test/...` запрещён: стандартный
  teardown Flutter может удалить base production package и его локальные
  данные даже при flavored APK.
- Текущий проверенный baseline (после ЧЕТВЁРТОГО раунда независимой
  перепроверки P2, 2026-07-16): backend — 72 unit tests (не изменялся);
  Flutter — 269 тест (было 263 после четвёртого раунда, 261 после третьего,
  255 после второго, 248 после первого,
  233 до перепроверки, 191 до P2), включая host/device smoke основного
  PDF → курс → упражнение flow.
  `flutter analyze` проходит без замечаний; `dart format
  --set-exit-if-changed .` чист; `flutter test --coverage` —
  2829/4822 строк (58,67%); production flavor release APK успешно
  пересобрана локально после CR-15 (не опубликована).
- P1 (CR-07 cloud sync outbox, CR-09/CR-10 device gate policy, CR-11 typed
  API errors, CR-12 Android privacy) закрыт 2026-07-15. Backend force-device
  и course-delete сохраняют `{ok:bool}`, но теперь подтверждают только
  реальный Firestore результат (`503 {ok:false}` при failure). Device
  integration smoke прошёл 1/1 на физическом Samsung SM-S938B через
  `tool/run_android_integration.sh`; production package остался установлен,
  integration package удалён.
- P2: CR-08 (typed DTO для всех 12 типов упражнений, backward-compatible
  schema v1) и CR-14 (bounded/atomic TTS cache) закрыты 2026-07-15, но
  независимая перепроверка в тот же день вернула **REQUEST CHANGES** по
  трём пунктам: конкурентная запись TTS-кэша (`TtsService.ensureAudio` —
  два параллельных запроса одной реплики могли гонять один и тот же
  `<key>.mp3.tmp` и падать с `PathNotFoundException`), lifecycle
  `DialogueAudioPlayer` (устаревшая async-цепочка после dispose/повторного
  запуска могла тронуть UI/воспроизведение) и неполнота CR-14 (не
  удалялся унаследованный `Documents/tts_cache` от версий до миграции).
  Все три исправлены в следующей сессии тем же днём: `_pendingByKey`
  сериализует конкурентные `ensureAudio`-операции по ключу (4 новых
  теста), `_opToken` защищает `DialogueAudioPlayer` от устаревших
  операций и paused-семантика исправлена (3 новых теста), one-time
  best-effort legacy-cleanup добавлен (4 новых теста). Заодно CR-08
  усилен: `ExerciseQuestion.number` (структурный identity-ключ,
  используемый как ключ answer-карт во всех exercise screens) раньше
  дефолтился в `0` при отсутствии — два вопроса без номера молча
  схлопывались в один; теперь бросает `ExerciseSchemaException` и
  проверяется на уникальность (4 новых/изменённых теста), legacy-fixture
  расширена с 10 до всех 12 типов упражнений. CR-08 и CR-14 теперь честно
  закрыты по полному списку требований, не только по первоначальной
  формулировке. В рамках описываемой здесь сессии CR-13 (Course/Exercise
  loader) и CR-15 (localization/accessibility) не трогались; это
  исторический срез, актуализированный ниже разделом от 2026-07-16. На тот
  момент оставались частично продвинутыми — DropdownButton gap-labeling и реальный
  TalkBack-прогон ещё не сделаны, хотя устройство было доступно в конце
  сессии (задача не входила в её объём). CR-16: только patch/minor
  Firebase bump выполнен; `go_router`/`google_fonts`/`device_info_plus`
  major-апгрейды оценены и сознательно отложены (высокий блaст радиус на
  device-gate и роутинг); `file_picker` заблокирован намеренно (`c53c20c`:
  11.x ломает сборку с текущим toolchain, 10.3.11 retracted) — см.
  `CODE_REVIEW_2026-07-15.md` «Независимая перепроверка P2 (CR-08/CR-14)».
- **Второй раунд независимой перепроверки (тем же днём) нашёл ещё два
  дефекта в только что «закрытом» CR-14** — первый раунд закрыл CR-14
  преждевременно. HIGH: `TtsService._enforceCacheBudget` вызывалась
  независимо каждым concurrent commit'ом РАЗНЫХ ключей без синхронизации
  между ними — два одновременных commit'а могли оба увидеть один и тот же
  over-budget снимок каталога и оба удалить файл, иногда оставляя кэш
  пустым там, где нужно было удалить ровно один. Исправлено новой
  глобальной очередью `_evictionChain` (`_commitAndEnforceBudget`,
  отдельной от per-key `_pendingByKey`), с параметром `exclude`,
  гарантирующим, что `ensureAudio()` никогда не возвращает путь, который
  его же собственный eviction-проход только что удалил; синтез для разных
  ключей остаётся параллельным, сериализован только commit+evict хвост. 2
  новых теста, проверенных как настоящий regression (временный откат
  воспроизвёл падение 6/6 прогонов). MEDIUM: `DialogueAudioPlayer._playFrom`
  переводил UI в `playing` до вызова `_player.play()`; сбой `play()`/
  `setPlaybackRate()` молча проглатывался, оставляя виджет с «играющим»
  баром без звука и без выхода. Исправлено единым try/catch → generic
  localized error state; добавлен минимальный injectable
  `AudioPlayerAdapter` (`@visibleForTesting debugPlayerFactory`, `null` в
  проде → настоящий `AudioPlayer`) — впервые позволяет тестам реально
  достигать `playing`/`paused`, в обход незамоканных `audioplayers`
  platform channels. 5 новых тестов. Заявлено (как выяснилось, преждевременно)
  «CR-14 теперь честно закрыт» — оба на тот момент известных раунда
  независимой перепроверки пройдены. См. `CODE_REVIEW_2026-07-15.md`
  «Второй раунд независимой перепроверки P2 (CR-14): concurrent eviction
  и DialogueAudioPlayer play-error state». Итоговый Flutter-тест-baseline
  после этих двух раундов: 255 тестов, coverage 51,05%.
- **Третий раунд независимой перепроверки (2026-07-16) нашёл, что
  гарантия возвращаемого пути всё ещё нарушалась** — второй раунд закрыл
  concurrent over-eviction МЕЖДУ проходами, но `exclude:
  justCommittedPath` защищал файл только от eviction-прохода СОБСТВЕННОЙ
  операции; более ранний проход ДРУГОГО cache key мог увидеть файл более
  поздней операции (уже на диске, но ещё не в очереди) и удалить его,
  пока та операция ждала своей очереди — `ensureAudio()` мог резолвиться
  уже удалённым путём. Детерминированный репро (два параллельных
  `ensureAudio()` разных ключей, `File(path).exists()` сразу по резолву)
  давал `[false, true]`; существовавший тест при этом требовал `existsA
  != existsB` — кодировал баг как ожидаемое поведение. Исправлено
  refcounted pin/lease-моделью: `TtsService._pinnedPaths` (глобальный,
  разделяемый между ВСЕМИ операциями набор «файлов на руках», не
  единичный `exclude` одного прохода), явный `releasePaths()` (без
  автоматического снятия в `finally` — это race'ило бы continuation
  вызывающей стороны по microtask ordering), единственный потребитель
  `DialogueAudioPlayer` обновлён release'ить lease на каждой точке
  завершения использования (успех/ошибка/stop/regenerate/dispose/stale).
  Попутно исправлен смежный race: `_jumpTo()` не бумпает `_opToken`, из-за
  чего устаревший `_playFrom` мог зарегистрировать completion-listener
  после более нового — добавлен независимый `_playSeq`. 11
  новых/переписанных тестов, оба ключевых блока проверены как настоящий
  regression через временный откат. См. `CODE_REVIEW_2026-07-15.md`
  «Третий раунд независимой перепроверки P2 (CR-14): нарушенная гарантия
  возвращаемого пути и lease-lifecycle DialogueAudioPlayer». **CR-14 теперь
  закрыт с доказанной гарантией возвращаемого пути** после трёх раундов
  независимой перепроверки. Итоговый Flutter-тест-baseline: 261 тест,
  coverage 52,44%.
- **Четвёртый раунд независимой перепроверки (2026-07-16) устранил два
  ownership-дефекта третьего решения CR-14.** Строковый `releasePaths()`
  позволял одному владельцу повторным release уменьшить refcount другого
  владельца того же пути; `clearCache()` мог физически удалить общий файл,
  пока другой плеер всё ещё держал pin. `ensureAudio()` теперь возвращает
  уникальный `TtsAudioLease` с идемпотентным на уровне владельца `release()`
  и `path`; единственный production caller `DialogueAudioPlayer` мигрирован
  на typed lease. Cache hit/commit/release eviction/clear сериализованы общей
  filesystem transaction queue, а `clearCache` сохраняет чужие активные
  lease. Добавлены 2 детерминированные regression-проверки; полный Flutter
  baseline — 263 теста. Backend API и формат кэша не менялись. CR-14 закрыт
  после четырёх раундов. Общий device smoke PDF → курс → упражнение прошёл
  1/1 на Samsung SM-S938B с сохранением production package; выделенный
  real-device TTS stress/error smoke всё ещё отсутствует. Backend gate —
  72/72 + `py_compile` во временном venv.
  Реализация Flutter: `4d1c668` (`phase5-account-deletion`); этот план:
  `d89e8cf` (`phase3-2-promptfoo-gate`), backend-код не менялся.
- **CR-15 gap accessibility продвинут 2026-07-16.** Оба Sprachbausteine
  dropdown объявляют локализованный реальный номер PDF-пропуска, сохраняют
  tap/value Semantics и имеют 48 dp target. Teil 1 хранит исходный marker
  отдельно от внутреннего индекса. Подтверждённые при 200% overflow +69/+27
  px устранены scale-aware selected presentation; host tests покрывают 4
  locale и русский UI при точном 200%. Safe device runner теперь выполняет
  PDF smoke 1/1 и backend-free CR-15 smoke 1/1. Ручное прослушивание TalkBack,
  high contrast и keyboard navigation остаются непроверенными, поэтому общий
  CR-15 всё ещё partial. Production package до device-прогона отсутствовал;
  integration package после него удалён. Реализация и Flutter-документация:
  `3bb1ec3` (`phase5-account-deletion`); backend-код не менялся.
- Актуальная точка передачи следующему AI-агенту, включая состояние Git,
  оставшуюся часть CR-13/CR-15/CR-16, acceptance criteria и безопасные
  команды: `/home/igor/project/exam_trainer/NEXT_AGENT_PROMPT.md`.
- Проверенный P0 Flutter baseline сохранён коммитом `276afdb`; дальнейшая
  реализация и независимая перепроверка описаны в Flutter handoff. Архивный AAB
  versionCode 10 к этому baseline не относится и не пригоден для публикации.

## Детальное описание программы

### Flutter follow-up status — 18 июля 2026

Клиентский CR-13 получил минимальный срез для FavoritesScreen: controller
с terminal error/retry состояниями и stale/dispose guards; прямой FutureBuilder
больше не оставляет вечный spinner. По CR-16 безопасно обновлён
`device_info_plus` до 12.4.0. Версия 13.x отложена из-за конфликта `win32` с
намеренно закреплённым `file_picker 10.3.10`; backend API и код backend не
менялись. Оставшиеся направления: ручной TalkBack/high-contrast audit,
следующие небольшие CR-13 slices и отдельная миграция file_picker.

Следующий CR-13 срез `CourseScreen` также закрыт на Flutter-клиенте:
`CourseScreenController` добавляет stale/dispose guards и reload при смене
course id, сохраняя terminal error/not-found/retry UI. Backend API и storage
контракт не менялись.

Post-bump Android gate выполнен на Samsung SM-G985F: safe runner PDF 1/1 и
Sprachbausteine accessibility 1/1, production package сохранён, integration
package удалён; свежий production APK установлен поверх существующего. Ручной
TalkBack/high-contrast/keyboard audit остаётся незакрытым.

Пользователь затем вручную проверил TalkBack на production APK на Samsung
SM-G985F и подтвердил корректную работу. CR-15 закрыт для целевой Android
touch/TalkBack-матрицы вместе с 48 dp, Semantics и 200% device/host gates.
High contrast и внешняя аппаратная клавиатура остаются optional расширением,
так как отдельно не проверялись; backend не менялся.

Следующий CR-13 срез также закрыт на Flutter-клиенте: общий FavoriteButton
получил controller с terminal error/retry, stale/dispose guards и блокировкой
двойного нажатия. Backend API, UID-ключи и формат favorites не менялись.

### 1. Назначение продукта

Exam Trainer — Android-first Flutter-приложение для подготовки к языковому
экзамену. Пользователь выбирает профиль экзамена, загружает собственный PDF с
экзаменационными материалами, а система превращает его в структурированный
курс с интерактивными упражнениями. Приложение не продаёт и не поставляет
чужой экзаменационный PDF: исходный материал приносит сам пользователь.

Текущая реально поддерживаемая комбинация — **telc Beruf B2**. В интерфейсе
уже перечислены будущие профили telc/Goethe, B1/B2/C1, Allgemein/Pflege, но они
помечены как недоступные: для них ещё нет отдельных discovery-промптов, схем и
проверенных тренажёров. UI локализован на немецкий, английский, русский и
украинский языки; язык по умолчанию — немецкий.

Кроме импортируемых курсов есть отдельный встроенный модуль устной части
`Sprechen B2 Beruf` с фиксированным банком заданий для Teil 1, Small Talk
(Teil 2) и Teil 3. Он не зависит от PDF, Gemini или кэша импорта.

### 2. Основной пользовательский путь

1. **Запуск.** Flutter сразу рисует брендированный bootstrap-экран, пока
   локально инициализируется Firebase. На Android 12+ используется отдельная
   splash-тема, поэтому холодный старт не должен показывать пустой чёрный
   экран.
2. **Вход.** Пользователь входит через Google или email/password. При новой
   регистрации он должен принять privacy policy и terms. Firebase Auth
   хранит сессию и выдаёт ID token для backend-запросов.
3. **Проверка устройства.** После входа приложение один раз за сессию вызывает
   `/api/device`. На аккаунт разрешено до двух устройств; экран лимита может
   удалить прежние регистрации и оставить текущее устройство. Сетевой сбой
   здесь fail-open и не блокирует пользователя.
4. **Главный экран.** Пользователь видит импортированные курсы, вход в модуль
   устной части, избранное, настройки языка и аккаунта. Курсы сначала читаются
   локально, затем best-effort дополняются курсами того же UID из Firestore.
5. **Импорт PDF.** Пользователь выбирает доступный профиль экзамена и файл.
   Приложение показывает этапы конвертации, проверки кэша, discovery,
   пораздельного parse и сохранения.
6. **Курс.** Результат сохраняется как `ParsedCourse`: UUID, имя исходного
   файла, дата импорта, профиль экзамена и словарь из 12 типов разделов со
   списками вариантов. Экран курса ведёт к разделу, варианту или режиму
   пробного экзамена.
7. **Тренировка.** Экран показывает исходный текст/диалог, вопрос и варианты
   ответа. Для слуховых заданий можно синтезировать немецкое аудио. Упражнение
   можно добавить в избранное.
8. **Удаление.** Удаление курса сразу очищает локальный JSON и связанные
   bookmarks, затем асинхронно удаляет облачную копию. Удаление аккаунта
   сначала очищает Firestore, затем Firebase Auth и только после подтверждения
   сервера удаляет локальные данные.

### 3. Архитектура системы

```text
Пользователь
    │
    ▼
Flutter-клиент ───── Firebase Auth (Google / email-password)
    │  HTTPS + Firebase ID token
    ▼
Flask API на Vercel
    ├── pdfminer + PyMuPDF ── PDF → нормализованный текст
    ├── Upstash Redis ─────── глобальный doc/discover/group кэш и лимиты
    ├── Gemini API ────────── discovery и структурированный parse
    ├── Firestore REST ────── тариф, устройства и облачные копии курсов
    └── Microsoft Edge TTS ── немецкое аудио
```

| Компонент | Ответственность | Основные файлы |
|---|---|---|
| Flutter UI | Навигация, импорт, тренажёры, локализация | `lib/app.dart`, `lib/screens/` |
| Клиентские сервисы | Auth, API, кэш-ключи, хранение, TTS, устройства | `lib/services/` |
| Vercel API | Auth boundary, лимиты и orchestration | `main.py` |
| PDF conversion | Текст и восстановление визуально отмеченных ключей | `answer_markers.py` |
| Gemini contracts | Промпты, модели и responseSchema | `prompts.py`, `generation_config.py`, `response_schemas.py` |
| Verbatim extraction | Line-span и механическая подстановка текста | `line_extraction.py`, `span_resolution.py` |
| Firebase/Firestore | Проверка токена, тариф, устройства, курсы, удаление | `firebase_auth.py`, `firestore_client.py` |
| Quality gates | Unit, promptfoo, E2E и сверка курируемого контента | `tests/`, `promptfoo/`, `scripts/`, `tools/` |

### 4. Пайплайн PDF → тренажёр

#### 4.1 Convert

Клиент отправляет бинарный PDF на `POST /api/convert`. Backend временно пишет
его в файл, извлекает текст через `pdfminer`, нормализует переводы строк и
удаляет временный файл в `finally`. Исходный PDF на сервере постоянно не
хранится.

Затем PyMuPDF читает визуальную разметку PDF. Если вариант ответа выделен
жёлтым прямоугольником, `answer_markers.py` находит соответствующую строку
`a)`/`b)`/`c)` в извлечённом тексте и добавляет маркер `– 100%`. Так ключ,
который иначе потерялся бы вместе с форматированием PDF, становится доступен
детерминированно. Этот post-pass best-effort: его сбой не должен ломать саму
конвертацию.

#### 4.2 Whole-document cache

Клиент вычисляет SHA-256 от точного конвертированного текста и сначала
проверяет doc-кэш `v30.v36|doc|<hash>`. Hit возвращает готовые 12 разделов и
полностью пропускает discovery/parse. Ключ чувствителен к любому изменению
текста или алгоритма convert; поэтому курируемый кэш мигрируется после
релевантного version bump.

#### 4.3 Discovery

При doc-cache miss клиент нумерует все строки документа и проверяет discovery
кэш `v30|discover|<hash>`. На miss весь документ один раз обрабатывает
`gemini-3.5-flash` при temperature 0. Модель возвращает только структуру:
`section_type`, `variant_number`, `version_label`, `start_line`, `anchor`.

`anchor` позволяет клиенту исправить потерянную цифру в `start_line`: строка
сверяется с реальным документом, а при расхождении ищется ближайшее совпадение.
Записи `other` обозначают оглавления, устную часть и прочие блоки, которые не
должны становиться упражнениями, но нужны как безопасные границы чанков.

#### 4.4 Grouping и parse

Клиент режет документ по discovery-границам и группирует все редакции одного
`(section_type, variant_number)` через `<<<ITEM>>>`. Каждый group сначала
ищется в кэше `v36|group|<hash>`. Cache miss отправляется на `/api/parse`; до
шести групп обрабатываются параллельно.

Parse использует `gemini-3.1-flash-lite`, per-type prompt и обязательный
Gemini `responseSchema`. Схема фиксирует поля, допустимые типы вопросов и
точное число вопросов там, где формат экзамена это определяет. Клиент после
ответа раскрывает dedup-сентинелы, проверяет структуру и ключи, повторяет
структурно плохой ответ и не кэширует невалидный результат. Редакции одного
варианта объединяются, но разные version labels сохраняются отдельно.

Для verbatim-полей модель не должна перепечатывать длинный исходный текст:

- `telefonnotiz` возвращает line-span для дополнительных сведений;
- `lesen_teil2` и `hoeren_teil4` возвращают line-span для `texts[].content`;
- backend механически вырезает указанные строки из исходного чанка,
  сохраняет абзацы/hyphen-wrap и только затем возвращает обычный контракт
  `{title, content}` клиенту.

Это уменьшает output-токены и делает перефразирование этих полей структурно
невозможным. Line-span не решает отдельную проблему потери целых редакций при
parse — она остаётся самостоятельной задачей.

#### 4.5 Сохранение результата

Полностью успешный Premium-импорт best-effort записывает assembled course в
doc-кэш. Сам курс всегда сначала сохраняется локально в
`documents/courses/<uid>/<courseId>.json`; облачная загрузка не блокирует UI.
Firestore хранит курс одним JSON-полем. Значения больше 900 КБ не отправляются
в облако из-за лимита документа Firestore, но продолжают работать локально.

### 5. Поддерживаемые разделы и экраны

| Ключ | Раздел | Экран |
|---|---|---|
| `lesen_teil1` | Lesen Teil 1 | universal |
| `lesen_teil2` | Lesen Teil 2 | universal + line-span текста |
| `lesen_teil3` | Lesen Teil 3 | universal |
| `lesen_teil4` | Lesen Teil 4 | universal |
| `beschwerde` | Beschwerde | специализированный |
| `sprachbausteine_teil1` | Sprachbausteine Teil 1 | специализированный |
| `sprachbausteine_teil2` | Sprachbausteine Teil 2 | специализированный |
| `telefonnotiz` | Hören + Schreiben / Telefonnotiz | специализированный + line-span |
| `hoeren_teil1` | Hören Teil 1 | специализированный диалоговый |
| `hoeren_teil2` | Hören Teil 2 | universal |
| `hoeren_teil3` | Hören Teil 3 | universal |
| `hoeren_teil4` | Hören Teil 4 | universal + line-span текста |

`ProbeExamService` собирает имеющиеся упражнения курса в последовательный
пробный проход. Его состояние (`current`, `visited`) сейчас живёт только в
памяти и сбрасывается при новом открытии; постоянной статистики, баллов и
истории прохождений пока нет.

### 6. Free и Premium

| Возможность | Free | Premium |
|---|---|---|
| Уже обработанный PDF | Да, через общий кэш | Да |
| Новый неизвестный PDF | Нет, понятный 403 | Да, до 5 новых документов/день |
| Варианты раздела | Первый вариант | Все найденные варианты и редакции |
| Telefonnotiz editions | Первая редакция | Все редакции |
| Parse-защита | До 12 000 символов и 8 редакций на group | Полный group |

Free-клиент при doc-cache hit не получает полный Premium-курс: результат
сокращается до первого top-level item каждого раздела, а список `versions` у
telefonnotiz — до первой редакции. Backend независимо повторно ограничивает
размер free parse, поэтому модифицированный клиент не обходит тариф.

Premium сейчас является boolean `users/{uid}.isPremium` в Firestore. В коде
нет Play Billing, подписки или экрана покупки; выдача Premium пока является
внешней административной операцией. Ошибка чтения тарифа всегда трактуется
как Free, а не как неявный Premium.

### 7. Аутентификация, данные и безопасность

- Каждый защищённый запрос несёт `Authorization: Bearer <Firebase ID token>`.
  Backend проверяет RS256-подпись, issuer и audience по Google JWKS и получает
  UID; общего секрета внутри APK нет.
- Локальные курсы и preferences namespaced по UID, поэтому смена аккаунта на
  одном телефоне не раскрывает курсы предыдущего пользователя.
- Firestore содержит `users/{uid}`, `devices`, `courses`; `progress`
  зарезервирован и пока не записывается. Backend обращается к Firestore REST
  через service account, чтобы не тащить тяжёлый Firebase Admin SDK в Vercel.
- Upstash-кэш глобальный и не привязан к UID, потому что одинаковый исходный
  текст должен давать одинаковый parse. В кэше хранится JSON результата, а не
  raw PDF. Rate-limit и дневные import caps также живут в Redis с TTL.
- Лимиты: 1000 API-запросов/час на UID, 5 новых discovery/день на Premium и
  100 новых discovery/день на весь сервис. Кэш-хиты не расходуют Gemini.
- Избранное хранится локально. При удалении курса связанные bookmarks
  удаляются каскадно, чтобы не оставлять мёртвые маршруты.
- Account deletion удаляет подколлекции `devices/courses/progress`, корневой
  документ пользователя и затем Firebase Auth account. Частичный сбой после
  удаления данных отдельно сообщается клиенту. Ручной E2E на физическом
  Android-устройстве пройден 2026-07-15: production вернул полный успех,
  приложение перешло на экран входа, локальные файлы курсов и scoped
  preferences отсутствуют. Это также подтверждает рабочее IAM-разрешение
  service account на удаление Firebase Auth user.

### 8. Аудио

Слуховые упражнения используют `/api/tts`. Клиент разбивает диалог по
говорящим и длинные реплики — на фрагменты до 400 символов. Backend выбирает
немецкий мужской/женский голос детерминированно по имени говорящего и
синтезирует MP3 через Microsoft Edge TTS. Клиент кэширует каждый фрагмент на
диске по хэшу, поэтому повторное прослушивание не требует нового запроса.

### 9. Публичные API endpoints

| Endpoint | Назначение |
|---|---|
| `GET /api/me` | Текущий тариф |
| `POST /api/device` | Проверить/зарегистрировать устройство |
| `POST /api/device/force` | Удалить другие устройства и оставить текущее |
| `GET/POST /api/courses` | Синхронизация курсов |
| `DELETE /api/courses/<id>` | Удаление облачного курса |
| `DELETE /api/account` | Данные Firestore + Firebase Auth account |
| `POST /api/convert` | PDF → нормализованный текст с answer markers |
| `POST /api/parse` | Discovery или parse одного group |
| `GET/POST /api/cache` | Общий versioned JSON cache |
| `POST /api/tts` | Текст → немецкий MP3 |

### 10. Границы текущей версии

- Основная целевая платформа и проверенный release — Android; остальные
  Flutter-платформы присутствуют в шаблоне, но не являются подтверждённым
  production-релизом.
- Поддержан только telc Beruf B2.
- Приложение не распознаёт скан без текстового слоя через OCR.
- Нет встроенной покупки Premium.
- Нет постоянной статистики прохождений и прогресса.
- Облачная синхронизация best-effort и пропускает курсы больше 900 КБ.
- Качество нового PDF зависит от discovery/parse; курируемый флагманский PDF
  обслуживается отдельно через проверенный doc-кэш.

### Следующие актуальные задачи

1. Внести `https://bodia2010.github.io/exam-trainer/delete-account.html` в
   поле Account deletion раздела Data safety в Play Console и вручную
   подтвердить фактический статус. Страница опубликована 2026-07-15 в
   GitHub Pages, отвечает HTTP 200 и проверена на наличие всех четырёх
   языковых версий. Доступа к Play Console в текущем рабочем окружении нет.
2. Подготовить телефонные скриншоты Play Store и feature graphic 1024×500.
   Для содержательных скриншотов после удаления тестового аккаунта нужно
   снова войти в приложение и восстановить показательный курс; без явного
   действия пользователя аккаунт заново не создавать. Новая APK установлена,
   но телефон заблокирован PIN-кодом, поэтому splash и экран входа после
   установки нужно визуально подтвердить после разблокировки.

Дата: 2026-07-12. Контекст: приложение публикуется и монетизируется;
модель «пользователь загружает свой PDF» остаётся (юридическая позиция:
продаём инструмент, контент приносит пользователь). **Оба ключа Gemini
теперь платные** (free-тариф больше не сидит на бесплатной квоте) — это
меняет приоритеты: защита от расходов нужна и для free-тарифа.

Опорные факты по стоимости (замерено сегодня на реальном документе):

| Вызов | Модель | Токены | Цена |
|---|---|---|---|
| discover (1 раз на новый документ) | gemini-3.5-flash | ~154K in + ~15K out/thoughts | ~$0.35 |
| parse (все 12 секций суммарно) | gemini-3.1-flash-lite | мелкие чанки | ~$0.10–0.20 |
| **Итого первый импорт нового PDF** | | | **~$0.50** |
| Повторный импорт того же PDF (кэш) | — | 0 | **$0** |

Кэш глобальный (ключ = хэш содержимого, без TTL, не привязан к
пользователю). Ниша узкая — большинство пользователей импортируют одну и
ту же компиляцию из Telegram-канала. Значит предельная стоимость
пользователя ≈ $0, платим только за «первопроходца» каждой версии PDF —
если закрыть дыры для злоупотреблений (Фаза 0) и перестать самим сбрасывать
кэш зря (уже починено: раздельные версии discover/parse кэша).

---

## Фаза 0 — защита кошелька (СНАЧАЛА, до публикации; ~полдня)

Сейчас rate limit — 1000 запросов/час на пользователя. Один discover
≈ $0.35 → злонамеренный аккаунт с уникальными PDF может сжечь сотни
долларов за час. Оба тарифа на платных ключах, поэтому:

### 0.1 Лимит импортов (discover-вызовов) в день на пользователя
- В `main.py::parse()` для `section_type == 'discover'` (только при
  кэш-промахе — кэш-хиты не считаются): Redis-счётчик
  `importcap|{uid}|{YYYYMMDD}`, expire 24ч.
- Premium: **5 новых документов/день** (реальному студенту хватает с
  запасом; потолок расходов $2.5/день на аккаунт).
- Free: см. 0.2.
- Ответ при превышении: 429 + понятное сообщение (клиент уже умеет
  показывать серверные ошибки лимитов).

### 0.2 Free-тариф: discover только по кэшу (рекомендуемый вариант)
Free-пользователь НЕ может запустить discover для документа, которого
нет в кэше — вместо этого получает сообщение «этот документ ещё не
обработан — доступно в Premium». Следствия:
- Free-тариф теперь стоит **строго $0** в Gemini-расходах на discover
  (parse для free и так ограничен 12K символов / 8 изданий — копейки,
  плюс тоже можно закрыть той же логикой «только по кэшу»).
- Популярную компиляцию зальёт первый же premium (или мы сами через
  Фазу 1) — free-пользователи получат полный опыт на главном контенте.
- Незнакомый PDF становится апселлом в premium — честная и понятная
  граница тарифа.
- Реализация: в `parse()` при кэш-промахе и `not premium` → 402/403 с
  кодом ошибки для клиента. Альтернатива (если вариант покажется
  жёстким): 1 импорт/день free — но это до $0.5/день на каждый free-
  аккаунт, при масштабе это дыра; вариант «только по кэшу» безопаснее.

### 0.3 Глобальный дневной предохранитель
Redis-счётчик discover-вызовов на весь сервис в день (например, 100).
При превышении новые документы получают «сервис перегружен, попробуйте
завтра», кэш-хиты продолжают работать. Защищает от координированного
злоупотребления множеством аккаунтов. Констант в одном месте, рядом с
`_RATE_LIMIT_PER_HOUR`.

### 0.4 Мониторинг расходов
- Еженедельно: `vercel logs ... | python3 scripts/cost_report.py`
  (уже считает по-модельно честно).
- В GEMINI_USAGE лог добавить `uid=` (усечённый) — чтобы при аномалии
  видеть, кто именно жжёт токены.

**Definition of done Фазы 0:** злонамеренный аккаунт любого тарифа не
может потратить больше $2.5/день; free-тариф стоит ~$0; есть глобальный
стоп-кран.

---

## Фаза 1 — курируемый флагманский контент через прогрев кэша (~1 день)

Идея: воркфлоу сверки (офлайн-парсинг → `verify_content.py` → ручные
правки) производит эталонный JSON, который **записывается в Redis под
теми же ключами, которые клиент вычислил бы сам**. Пользователь
импортирует популярный PDF → хэш совпал → мгновенно получает выверенную
версию за $0. В приложении и API не меняется ни строчки.

### 1.1 `tools/inject_curated.py` ✅ 2026-07-14
- Вход: markdown источника + курируемый course JSON (+ версии кэша).
- Пишет **один ключ уровня `doc`** (кэш целого курса —
  `getCachedSections` проверяет его первым, при хите discover/parse
  вообще не вызываются): `{discoverVer}.{parseVer}|doc|sha256('doc|'+markdown)`.
- Порт хэширования из `parse_service.dart` (sha256 utf-8, формат входа
  `doc|<markdown>`); обязательная самопроверка: прочитать из Redis
  существующий ключ, записанный клиентом, и убедиться, что вычисленный
  скриптом ключ совпадает байт-в-байт.
- **Критичная деталь:** markdown должен быть байт-идентичен тому, что
  выдаёт продовый `/api/convert`. Инструмент воспроизводит текущий
  production-путь (pdfminer-нормализация + PDF answer markers) и
  fail-closed требует PyMuPDF: без markers нельзя молча вычислить другой
  hash. Всегда сверять source/target и read-back до завершения миграции.
- По умолчанию инструмент работает как dry-run. Запись требует `--apply`,
  существующий отличающийся target никогда не перезаписывается, после SET
  выполняются побайтовый read-back и повторная проверка JSON-структуры.
- Flagship doc-кэш перенесён в v35 2026-07-14: 539155 bytes, 12 типов
  секций, SHA-256 значения
  `475627e6ac594155bb589de30b2352d72579cc8167589a19d685bb0f365d791b`.
  Read-back совпал. Промежуточный v34 для этого PDF ранее не создавался.
- После полной ручной сверки flagship doc-кэш опубликован в новый v36
  2026-07-15: 12 типов, 142 item, 525441 байт canonical serialized sections,
  SHA-256 `756c8ede5af8dbd3bd2da0e5b41e1d5d789710d09c6ce155b6426ce1889ff4a4`.
  Preflight подтвердил MISS, POST завершился успешно, немедленный read-back
  совпал, отдельный повторный GET вернул `already-matches`; v35 не изменялся.
  Подписанный APK `1.0.0+5` собран и установлен на физический Android;
  SHA-256 файла и установленного base APK совпадает:
  `90227fc4546877ffae9a0a1444a2373a9c4815a8d5e11b54c401cb99671a9e87`.

### 1.2 Курируемый контент v1 (по сегодняшнему отчёту сверки)
Основа — `course2.json` (уже вытащен с телефона). Исправить
подтверждённое:
- telefonnotiz: 3 случая обрезания `weitere_informationen` по «/»
  (восстановить полный текст буллетов из источника);
- telefonnotiz: «вариант №3/1» — сейчас это отдельный item с
  variant_number=31; переделать в издание варианта 3 (как пайплайн сам
  сделал с «№1/1»);
- hoeren_teil4 вариант 8: удалить галлюцинированные транскрипты и
  выдуманные ключи ответов (в источнике их нет) — оставить честное
  «(nicht angegeben)» / пометку «без ответов в источнике»;
- beschwerde вариант 1, Q19: ключ b → c (проверено по письму Lassner И
  по жёлтой разметке в PDF — стр. 65);
- ВСЕ ключи ответов сверить автоматически по PDF-разметке
  (`scripts/check_answer_keys.py`, см. 2.3.1) — заменяет ручную проверку
  спорных случаев; уже подтверждено разметкой: lesen_teil4 в.2 Q17 обе
  редакции извлечены неверно;
- разобрать lesen_teil2 (6+ items с перепутанными текстами/вопросами —
  самое серьёзное);
- перегнать сверку hoeren_teil2 (сегодня упал с 503).

Актуализация 2026-07-15: через авторизованную сессию подключённого Android-
устройства извлечён текущий production doc-кэш v35 и воспроизведён точный
`source.md` из исходного PDF. Курс содержит 12 типов и 142 item. Полная
локальная сверка ключей дала `243 OK`, `13 NO_MARK`, `1 AMBIGUOUS`,
`0 MISMATCH`; прежние 21 ошибки ключей в production уже исправлены. Полный
verbatim-аудит впервые охватил все item и оставил 61 кандидат на ручную
сверку: 32 отсутствующих дословных совпадения и 29 подозрительных обрезаний
по `/`. LLM-аудит не запускался, чтобы не тратить платную квоту без отдельного
решения.

Ручная сверка завершена 2026-07-15. Подтверждены четыре изменённых item:
две редакции `beschwerde` без напечатанных вопросов переведены на штатный
sentinel `(nicht angegeben)`, у `hoeren_teil3` удалён один выдуманный
дистрактор, у `hoeren_teil4` исправлено `the Homepage` → `die Homepage`.
Остальные срабатывания документированы как корректные альтернативы редакций,
heading/line-wrap normalization, штатный `Kein Text passt` или cloze-разметка.
14 fail-closed overrides применились к точным `old`; повторный checklist
проверил 4 изменённых item и дал `0 MISMATCH`, `0 HALLUCINATED`,
`0 TRUNCATED_AT_SLASH`. Проверенный результат опубликован только в новый v36.

### 1.3 Правки — в `overrides.json`, а не в сгенерированный файл ✅ 2026-07-15
Формат: список патчей `{path, old, new, reason}` + скрипт
`tools/apply_overrides.py`. Патч дополнительно адресует `section` и
`variant`; path поддерживает стабильные селекторы вроде
`questions[number=36].answer`. `old` сверяется строго, неоднозначная цель,
дрейф значения и повторный target завершают применение с ошибкой. Поэтому
пересборка курса не теряет ручные исправления и не может молча наложить их
на другой item. Фактический production v35 и audit-артефакты восстановлены
локально вне репозитория в `~/Downloads/exam-trainer-curation/`. Предыдущий
сырой doc-кэш v32 возвращает miss, а исторический `overrides.json` не
сохранился; поэтому для новых патчей допустимы только фактически проверенные
`old` из текущей production-базы, не реконструкция по памяти.

### 1.4 Правило поддержки
При каждом бампе версии кэша — перезалить курируемые записи
(`inject_curated.py` под новую версию). Одна строка в чек-лист релиза.

**Definition of done Фазы 1:** импорт флагманского PDF любым
пользователем возвращает выверенный курс мгновенно и бесплатно; ручные
правки переживают пересборку.

---

## Фаза 2 — детерминизация пайплайна для незнакомых PDF (инкрементально, 1–2 недели)

Для «длинного хвоста» чужих документов. Каждый шаг проходит через
promptfoo-гейт (`run_all.sh`) с новыми regression-фикстурами.

### 2.1 Line-span для verbatim-полей
Сейчас модель ПЕРЕПЕЧАТЫВАЕТ дословные тексты (монологи, письма,
транскрипты) — отсюда перефразирование, галлюцинации и большие
output-токены (самые дорогие). Переход: чанк отправляется с
пронумерованными строками (как уже сделано в discover), схема возвращает
`{start_line, end_line}` вместо текста, клиент вырезает текст кодом.
- Перефразирование/галлюцинация на этих полях становятся структурно
  невозможными; output-токены падают в разы → дешевле.
- Порядок внедрения по плотности багов: telefonnotiz (буллеты!) →
  lesen_teil2 (перепутанные тексты) → hoeren_teil4 (галлюцинация) →
  остальные.
- На каждый тип: схема + промпт + клиентская вырезка + фикстура + бамп
  `_parseCacheVersion` (+ перезаливка курируемого, см. 1.4).
- ✅ telefonnotiz `weitere_informationen` — сделано 2026-07-14
  (`line_extraction.py`), включая `slash_index` (общий ответ-блок на
  несколько изданий, значения через "/") — не предвиделось изначальным
  планом, обнаружено по ходу работы на реальных данных. Проверено
  двумя живыми promptfoo-регрессиями (обычный сплит + сентинел «нет
  буллетов»).
- ✅ lesen_teil2/hoeren_teil4 `texts[].content` — сделано 2026-07-14:
  per-type span-схема не меняет остальные universal-типы, `extract_block`
  сохраняет абзацы/hyphen-wrap и механически добавляет HEADINGS,
  `-1,-1` обозначает отсутствующий в источнике текст. Контракт
  клиента остался `{title, content}`, parse-cache поднят до v35.
- 📊 Live promptfoo 2026-07-14: все 5 спанов возвращённого hoeren_teil4
  прошли assertion. Тогдашний `4/5` был не edition-loss, а ошибочным
  ожиданием теста: три `<<<ITEM>>>` в fixture — correction-чанки вопросов
  36/38 одного варианта, а не три издания. Правильный результат — ровно
  один объект; gate исправлен 2026-07-15 и теперь проверяет exact count.
  Остальные зафиксированные 503/provider timeout были внешними сбоями.

### 2.2 Ключи ответов регэкспом, где есть маркер
Источник помечает правильные ответы маркерами «– 100%» / «-100%» /
«(100%)» / буквой после вопроса. После line-span у клиента есть точный
текст — извлекать ключ регэкспом БЕЗ LLM. В JSON добавить
`answer_source: "marker" | "model"`:
- «marker» — детерминированный, доверяем;
- «model» — вопросы на понимание текста (beschwerde Q19/20, lesen_teil4
  Q14–18 — Protokoll/письма, маркеров в источнике нет) — кандидаты на
  ручную проверку в курируемом контенте, UI может помечать их менее
  жирной уверенностью.

### 2.3 Извлечение ключей из PDF-разметки (highlights/underline)
**Открытие 2026-07-12 (проверено на реальном PDF):** правильные ответы
для ВСЕХ типов вопросов физически размечены в PDF жёлтой заливкой +
подчёркиванием — включая beschwerde и lesen_teil4, где текстового
маркера «– 100%» нет. MarkItDown выбрасывает форматирование, поэтому в
markdown ключ отсутствует и модель вынуждена угадывать — отсюда все
сегодняшние «неверные ключи». Это не задача рассуждения, а потеря
данных на этапе convert. Никакой сильной модели не нужно.

Извлечение (подтверждено PyMuPDF на стр. 65 и 47/49): жёлтые filled-
rect'ы (fill 1,1,0, высота >5pt) поверх строк опций; `page.get_text
(clip=rect)` возвращает точный текст опции. Семантика цветов: жёлтый на
строке-опции = ключ ответа; жёлтый на заголовке = просто выделение
заголовка; зелёный = мета-метки («Новый вариант от ...») — фильтровать
по паттерну строки-опции `^[a-c]\)`.

Реализация в два этапа:
1. **Офлайн сразу** (СДЕЛАНО 2026-07-12): `scripts/check_answer_keys.py`
   — вытаскивает все подсвеченные опции из PDF и детерминированно
   сверяет каждый ключ в course JSON. Результат первого прогона по
   актуальному курсу: **257 вопросов → 222 OK, 21 MISMATCH (~8% ключей
   неверны!), 13 без разметки в источнике, 1 неоднозначный.** Все
   ~9 «спорных» заявок LLM-аудитора подтвердились + найдено ещё ~12,
   которые аудитор пропустил (hoeren_teil3 в.7, hoeren_teil4 в.6/7/8,
   lesen_teil4 в.3/6). Полный список: scratchpad
   `courses/answer_keys_audit.txt` → все 21 идут в overrides
   курируемого контента (Фаза 1.2).
2. **В рантайм-convert позже**: post-pass после MarkItDown — найти
   подсвеченные сниппеты в markdown (нормализованный поиск строки, та же
   техника, что anchor-коррекция) и дописать существующий маркер
   «– 100%» к строкам-опциям. Все текущие правила промптов уже доверяют
   этому маркеру — они продолжают работать без изменений; regex-
   извлечение из 2.2 начинает покрывать и beschwerde/lesen_teil4.
   Требует: pymupdf в requirements (проверить лимит размера функции
   Vercel, wheel ~50MB), бамп версии кэша, promptfoo-гейт.

### 2.4 Нормализация «вариант №X/Y»
✅ Сделано 2026-07-13: правило SPLIT-SLASH NUMBERING в DISCOVER-промпте
(`prompts.py`) + синтетическая regression-фикстура
(`discover_split_slash_variant_number.txt`) + assertion в promptfoo.
Верификация прогоном promptfoo заблокирована временной перегрузкой
Gemini (503 напрямую от API, не наша проблема) — код готов, прогнать
`npx promptfoo eval` на этой фикстуре при следующей работе с промптами.

### 2.5 Известная проблема: промпт заставляет галлюцинировать, когда источник не даёт ответа
Найдено 2026-07-13 через `check_verbatim_content.py`: beschwerde
вариант 6, издание 2 («Новый вариант от 01.2025», Cavone GmbH) — в
источнике для этого издания физически нет напечатанных вопросов 19/20
(текст обрывается на следующем варианте). Правило в `prompts.py`
("every required question slot must still get its best-supported
answer from context... rather than omitting the question") напрямую
инструктирует модель придумать что-то, а не оставить вопрос пустым —
и именно так родились полностью выдуманные варианты ответов
("liegt an der Cavone GmbH." и т.д., не существующие в PDF).

Это НЕ одноразовый баг в данных (в отличие от hoeren_teil3/4 варианта
7, которые пофикшены заменой на sentinel) — это структурный конфликт:
"answer" в схеме для questions — обязательное поле (`response_schemas.py`
universal schema, `options` — ровно 3 записи), нет валидного способа
пометить "источник не дал ответа" без ломки формы. Нужно решение на
уровне схемы/промпта: либо разрешить `answer: null` для questions без
маркера в источнике (и научить клиент показывать «нет данных» вместо
вопроса), либо — раз это единичный, не повторяющийся кейс на всём
документе — оставить как есть и просто исключить эту пару вопросов
вручную в courated-патче. Не чинить наспех: тронуть эту UNIVERSAL-схему
затронет ВСЕ 9 типов секций, которые её используют.

**Definition of done Фазы 2:** verbatim-поля никогда не расходятся с
источником; ключи с маркером извлекаются без LLM; comprehension-ключи на
сильной модели; promptfoo зелёный.

---

## Фаза 3 — сверка как релизный гейт контента (~2–3 дня)

`verify_content.py`:
- ✅ `--runs N` (по умолчанию 3) + агрегация консенсуса: расхождение
  попадает в отчёт с пометкой «в скольких прогонах из N найдено»
  (подтверждено: одиночный прогон пропускает реальные баги и иногда
  галлюцинирует сам);
- ✅ retry на 503.

✅ `scripts/curation_checklist.py` — единая команда для «parse → verify»
части чек-листа: считает diff старого/нового course.json (через
`update_curated_content.py`), сужает review scope до реально
новых/изменённых items, и прогоняет по этому scope все три сверочных
инструмента (`check_answer_keys.py`, `check_verbatim_content.py`,
`verify_content.py --runs 3`) — один консолидированный отчёт,
детерминированные находки отдельно от LLM-подозрений. Ничего не
патчит автоматически.

**Обязательный шаг перед публикацией курируемого контента в Redis**
(тот самый «чек-лист релиза»):
1. `curation_checklist.py --old-course <текущий прод> --new-course <готовый курируемый курс> --pdf ... --source-md ... --out report.txt --receipt receipt.json`
2. Прочитать отчёт целиком — 0 findings не значит «готово», значит
   «читай LOW-CONFIDENCE секцию тоже»
3. Правки — в `overrides.json` (см. 1.3), не в сгенерированный файл
4. Только после этого — `inject_curated.py --course ... --checklist-receipt
   receipt.json --checklist-report report.txt --checklist-source-md source.md
   --apply` + выборочная проверка в приложении. Receipt содержит
   hashes точного serialized course, PDF, source markdown и отчёта. Инжектор
   fail-closed отклоняет другой course/PDF и незавершённые deterministic
   answer-key/verbatim шаги. Byte-identical перенос старого doc-cache между
   версиями без `--course` не считается публикацией нового контента и не
   требует receipt.

---

## Фаза 4 — прод-гигиена (после публикации, по мере роста)

- Экономика подписки: цена premium ≥ стоимости 5 импортов/день × месяц
  в худшем случае (~$75) не нужна — реальное потребление на порядки
  ниже; достаточно следить за cost_report еженедельно первое время.
- Апселл-экраны на лимитах (импорт-кэп, «документ не в кэше» для free).
- Позже, если появятся тяжёлые пользователи: батч-обработка новых PDF
  (Gemini batch API, −50%) — пока не нужно.

---

## ✅ Закрытые ложные edition-loss в promptfoo (2026-07-15)

Полный прогон `promptfoo/promptfooconfig.parse.yaml` (запущен для
проверки сегодняшних telefonnotiz-правок) проваливал `hoeren_teil1` И
`hoeren_teil4` на реальных fixture'ах — расследовано до конца,
результат разный для каждого:

- **hoeren_teil1 — ложная тревога, причина в тест-харнессе; исправлено.**
  Promptfoo при загрузке `file://fixtures/hoeren_teil1.txt` применял
  JavaScript `trim()` и терял начальные
  символы `\x0c` (form feed — известный артефакт разрыва страницы
  markitdown/pdfminer, см. `answer_markers.py`'s `SourceIndex`).
  Продовый пайплайн эти символы НЕ чистит — они доходят до Gemini как
  есть. Прямой вызов с form-feed на месте (то есть байт-в-байт как в
  проде): 6/6 прогонов вернули правильные 2 издания. Без form-feed
  (как грузит promptfoo): 2/2 провала. Вывод: тест кормит модель
  нерепрезентативным входом, продакшена это не касается. Исправление
  2026-07-15: оба promptfoo-конфига передают обычный `markdown_path`, а
  локальный `fixture_loader.py` читает UTF-8 bytes без trim и без
  universal-newline преобразования. Prompt-функции и assertions используют
  один и тот же raw-текст. Unit-тест фиксирует сохранение начального `\x0c`,
  CRLF и завершающих переводов строк; runtime/cache/schema не менялись.
- **hoeren_teil4 — тоже ложная тревога, причина в ожидании теста.**
  Fixture действительно имеет три `<<<ITEM>>>`, но второй и третий блоки
  начинаются с `Варианты ответов` для уже существующих вопросов 36 и 38,
  после чего продолжают тот же вариант №1. Это correction-чанки одного
  упражнения, не самостоятельные издания. Сам production prompt прямо
  требует выбрать пригодный answer block и вернуть ровно один объект с
  вопросами 36–40. Результат 1/3 был корректным; неверным был
  `min_items: 2` в promptfoo. Gate заменён на `min_items: 1` плюс
  `expected_items: 1`, поэтому теперь он отвергает и потерю объекта, и
  ошибочное раздвоение correction-чанков. Runtime/cache/schema менять не
  требуется.

---

## Порядок работ и что уже готово

| Шаг | Статус |
|---|---|
| Раздельные версии кэша discover/parse | ✅ сделано сегодня |
| cost_report.py с честными по-модельными ценами | ✅ сделано сегодня |
| verify_content.py + первый полный отчёт сверки | ✅ сделано сегодня |
| course2.json вытащен, подтверждённые баги описаны | ✅ сегодня, отчёт в scratchpad |
| **0.1–0.3 спенд-кэпы** | ✅ сделано 2026-07-13 |
| **1.1–1.4 инъекция курируемого контента** | ✅ doc-ключ + 12 free group-ключей + 3 общих group-ключа 2026-07-13; `tools/inject_curated.py` и doc-миграция v35 ✅ 2026-07-14 |
| 2.1–2.4 line-span/regex/модель | telefonnotiz ✅ 2026-07-14 (line-span+slash_index); lesen_teil2/hoeren_teil4 line-span ✅ 2026-07-14; PDF-highlight-маркер ✅ 2026-07-14 (все типы) |
| 3 сверка как гейт | ✅ `curation_checklist.py` + hash-bound receipt; `inject_curated.py --course --apply` fail-closed требует его с 2026-07-15 |

Принятые решения (2026-07-12):
1. ✅ Free-тариф: discover только по кэшу (незнакомый PDF = апселл).
2. ✅ Premium: кэп 5 новых документов/день.
3. ✅ Сильная модель для beschwerde/lesen_teil4 НЕ нужна — ключи
   размечены в PDF (жёлтая заливка + подчёркивание) и извлекаются
   детерминированно; см. 2.3.

---

## Flutter device-fix: startup redirect overlay (2026-07-16)

На новой установке Flutter-клиента воспроизведён бесконечный preload для
неавторизованного пользователя. Backend не вызывался и не был причиной:
GoRouter уже разрешал `/` в `/login`, но внешний overlay наблюдал исходный
route-information provider, а не итоговую конфигурацию delegate.

Клиент теперь слушает `routerDelegate.currentConfiguration`; добавлен
регрессионный тест реального redirect. Gate клиента: 271/271 тестов,
coverage 2849/4841 (58,85%), analyze чист. Исправленный production APK
дважды холодно запущен на Samsung SM-G985F и открыл Login без crash.
Backend API, Firebase Auth, UID-изоляция, Free/Premium и backend-код не
менялись.

---

## TTS: корректный женский голос Andrea (2026-07-16)

> Исторический промежуточный фикс; заменён staged rollout ниже. Backend
> больше не содержит списков конкретных имён.

Клиент теперь извлекает `Andrea Faber` из «Hallo, hier ist Andrea Faber» и
передаёт это имя существующим полем `speaker`; новый speaker также обходит
старый empty-speaker cache key. Backend добавил `andrea` в женский набор
`_gender`, поэтому `voice_for` выбирает только `FEMALE_VOICES`. Контракт
`/api/tts`, auth/rate-limit и формат аудио не менялись.

Проверено отдельными client/backend regression tests: Flutter 274/274,
coverage 58,87%, backend 73/73. Реальное прослушивание остаётся подтвердить
на авторизованном телефоне с соответствующим курсом.

## TTS: staged voice_gender rollout (2026-07-16)

Backend `/api/tts` теперь принимает необязательное JSON-поле
`voice_gender: "female" | "male" | "unknown"`. Отсутствующее поле и явное
`"unknown"` сохраняют совместимость: выбор идёт по явной роли speaker
(`Frau`/`Herr` и gendered roles), затем по общему пулу. Явные
`"female"`/`"male"` переопределяют эти эвристики; некорректное значение
возвращает безопасный `400` JSON без raw exception.

Parser/schema могут добавлять optional `metadata` с теми же gender-значениями:
`metadata.voice_gender` для монологов/сообщений и
`metadata.speaker_voice_genders[]` для диалогов с явными speaker labels.
Старые course JSON без metadata остаются валидными. Span resolution для
`lesen_teil2`/`hoeren_teil4` и `telefonnotiz` сохраняет только валидные
metadata-поля и безопасно отбрасывает malformed hints, не меняя legacy
`{title, content}` / `weitere_informationen: [string]` формы. Backend-списки
конкретных имён удалены: новые имена обслуживаются parser metadata, а
сомнительные случаи остаются `unknown` и могут быть исправлены manual override.

Flutter-клиент поддерживает этот staged контракт: `VoiceGender` defensively
парсит metadata, manual override имеет приоритет над parser hints, затем идёт
explicit `Frau`/`Herr`, затем `unknown`. TTS cache key переведён на v2 с
gender-компонентом, чтобы исправленный выбор голоса не переиспользовал старый
MP3. Parse cache поднят до `v37`. UID-isolated voice preferences хранятся в
SharedPreferences с latest-wins записью, не создаются без UID и очищаются при
account deletion. Ключ также включает course ID и устойчивую позицию записи.

UI показывает localized Automatic/Female/Male controls, включая per-speaker
controls для диалогов. Переключение text/recording/metadata/override в
`DialogueAudioPlayer` останавливает playback, invalidates stale operations,
releases typed leases, reparses lines and stays idle (no autoplay). Stable
recording IDs есть для Telefonnotiz editions, Hören Teil 1 pairs и universal
Hören text cards; duplicate/empty/Cyrillic labels не сталкиваются. Проверено:
Flutter 299/299, coverage 61.33%; backend 86/86 + `py_compile`; device PDF
integration прошёл. Production APK собран, установлен и cold-launched без
crash. Manual TTS listening после авторизации ещё требуется; backend deploy не
выполнялся.

## Cost-aware Promptfoo gate для TTS metadata (2026-07-16)

Paid gate больше не повторяет огромный discovery fixture автоматически.
`promptfoo/run_all.sh` требует явный mode: `--parse-only` запускает 18 cases;
`--discover-only` делает ровно один discovery pass, а `--full-release`
сначала требует зелёный parse и только затем запускает discovery. Любая
ошибка fail-closed; `--dry-run` позволяет бесплатно проверить выбранный scope.
Для текущего TTS rollout DISCOVER prompt/schema/model config не менялись,
поэтому change-relevant gate — parse-only.

Существующие parse-вызовы теперь доказывают новый контракт, не увеличивая
число запросов: Hören Teil 4 проверяет ожидаемую последовательность
`voice_gender`, Hören Teil 1 — точные speaker labels без пропусков/дубликатов/
выдуманных speakers, Telefonnotiz — metadata внутри каждой `versions[]`, а
Lesen Teil 1 — отсутствие TTS metadata. Custom provider возвращает Promptfoo
фактический `usageMetadata`, включая thinking/cached tokens, и оценку стоимости
для двух production-моделей; секреты, raw response body и fixture content в
ошибки не попадают. Ожидаемый paid scope этого rollout уменьшен с обычных 21
вызова (~$0.45–0.55) и прежнего worst case 27 вызовов (~$1.15–1.25) до 18
вызовов (~$0.10–0.20).

Discovery остаётся обязательным при изменении `DISCOVER`, `DISCOVER_SCHEMA`,
его model/generation config или discovery processing. Перед production deploy
также обязательна миграция curated/full-course cache с parse `v36` на `v37`,
иначе Free-пользователь не получит hit для уже курируемого документа.

### Первый live parse gate и remediation (2026-07-16)

Подтверждённый `--parse-only` pass выполнил ровно 18 Gemini Flash-Lite calls;
discovery и retry не запускались. Фактический usage: 53,050 input, 36,746
candidate-output и 7,777 thinking tokens, всего 97,573; provider оценил
Standard-tier стоимость в `$0.080047`. Результат 14/18 — release заблокирован,
как и должен делать fail-closed gate.

Четыре failures проверены по сохранённому локальному eval без повторного spend:

- Lesen Teil 2 вернул корректные headings непосредственно перед body spans,
  отделённые одной пустой строкой. Runtime теперь детерминированно расширяет
  span на такой соседний heading (не более двух строк, только через blank gap),
  сохраняя fail-closed для дальних/отделённых контентом значений. Captured
  response replay: 4/4 текста разрешаются без sentinel.
- Hören Teil 1 схлопнул шесть `<<<ITEM>>>` fragments в один объект и склеил
  speaker turns. Тип переведён с MINIMAL на LOW thinking; gate теперь требует
  минимум три полные editions, обязательные speaker sets и сохраняет строгие
  exact-label voice checks. Headerless fragments наследуют последний явно
  указанный номер варианта и могут образовать допустимую четвёртую edition.
- Telefonnotiz ошибочно превратил `variant 3/1` в `variant_number: 31` и вынес
  edition в отдельный объект. Prompt получил явное правило `N/M → N`; gate
  требует один variant 3 с пятью editions и пятью contextual male hints.
- Hören Teil 4 правильно определил фактического male narrator вместо
  отсутствующей Frau Plassberg, но ошибся для явно указанной Frau Bernhardt.
  Voice rule теперь требует сначала определить фактического говорящего по
  self-identification и matching question/answer block. Fixture expectation:
  `female, male, female, male, female`; конкретные имена в production prompt
  не добавлялись.

После remediation выполнялись только change-focused checks. Telefonnotiz стал
зелёным; H4 исправляется детерминированно по same-speaker `Frau/Herr` evidence
и прошёл replay трёх оплаченных outputs; H1 сохраняет speaker turns и даёт 3
обязательные полные editions, иногда плюс четвёртую headerless edition. Оба
варианта допустимы: минимум 3, один явный source variant number, три required
speaker sets, optional `Chef/Frau`, без placeholder content.

Последняя попытка offline replay через Promptfoo `--model-outputs` неожиданно
не отключила custom Python provider: Promptfoo автоматически загрузил локальный
`.env` и сделал ещё один полный live run. Не использовать этот флаг как
гарантию отсутствия spend для данного config. Этот run дал 17/18; единственный
H1 failure был старым чрезмерным exact-count=4. Его сохранённый output содержит
3 полные editions и проходит текущие assertions, поэтому regrade текущего
последнего набора — эффективные 18/18 без нового API-вызова.

Суммарная оценённая стоимость всех содержательных live calls этой remediation:
`$0.222686`. Две попытки H1 на 3.5 Flash завершились 503/timeout до usage и
показали `$0`; сильная модель отменена, H1 остаётся на Flash-Lite + LOW.
Production deploy, cache migration, commit и push на момент этой записи ещё не
выполнялись.

### Curated cache v37 pre-release (2026-07-16)

Проверка production-equivalent конвертации в изолированном release venv
воспроизвела исходный digest `a758e73f...` при PDF SHA-256
`53634b0c...`, поэтому `pdfminer.six` закреплён на `20260107`. Byte-identical
копирование v36 запрещено: исходный curated course содержал 0 voice metadata.
Сформирован `course_curated_v37.json` из прежнего проверенного курса с
ограниченным overlay только для оплаченных/regraded совпадающих блоков:
Hören Teil 1 variant 2 (оба curated editions), Hören Teil 4 variant 1 и
Telefonnotiz variant 3. Остальные 138 элементов оставлены byte-identical и
сохраняют legacy fallback.

Checklist receipt для v37: 4 changed items, 138 reused, answer-key check OK,
verbatim check OK (`SPLIT_ACROSS_EDITIONS` 11), 0 deterministic findings,
LLM audit skipped because no key was exported in the local curation shell.
Receipt и generated course находятся вне репозитория в
`/home/igor/Downloads/exam-trainer-curation/`; перед Redis write receipt
должен быть проверен `inject_curated.py --course` с явными `v36 → v37`.
На первом локальном запуске `vercel env pull --environment=production`
вернул пустые masked values, поэтому миграция была временно остановлена. После
получения REST credentials из Upstash Console выполнены dry-run и apply с
read-back verification; Redis target v37 теперь опубликован.

### Release completion (2026-07-16)

Кодовый commit `b20b9f8` запушен в `phase3-2-promptfoo-gate`. Production Redis
получил metadata-aware `v30.v37|doc|a758e73f...` (12 section types, 142 items,
30 voice metadata nodes; read-back SHA совпал с checklist receipt). Старый v36
не перезаписывался.

Staged Vercel deployment `dpl_HKLsh24MUykL8GjwXYdA3cGJ7xpo` собран в Ready,
проверен на уникальном URL через protection bypass и promoted на
`exam-trainer-api.vercel.app`. Production smoke: OPTIONS для convert/parse/cache/tts
вернули 200 JSON; unauthenticated `/api/me` и `/api/cache` вернули безопасный
401 JSON. Credentials после миграции удалены из временного локального файла.

### Flutter CR-13 SectionListScreen — 2026-07-18

Клиентский `SectionListScreen` переведён на lifecycle-safe
`SectionListController`; backend API, Redis-формат и production deployment не
изменялись. Проверены terminal error/not-found/empty states и защита от stale
операций/dispose; backend regression suite остаётся без изменений.

### Flutter CR-13 practice exam / CR-16 cleanup — 2026-07-18

Клиентский Probe-Prüfung loader переведён на controller с terminal error/retry и
lifecycle guards; `google_fonts` удалён как неиспользуемая зависимость. Backend API,
cache contract и production deployment не менялись.

Device-limit UI error/lifecycle hardening выполнен только в Flutter-клиенте;
backend device registration contract не изменён.

### Flutter CR-16 file_picker verification — 2026-07-18

Upgrade до 11.0.2 откатан после воспроизводимого clean Android release failure
на границе AGP9 built-in Kotlin/legacy KGP. Клиент остаётся на точном 10.3.10;
stable device-info 13 также несовместим с picker 11 по `win32 ^6` против `^5`.
Backend API, upload contract и production deployment не менялись.

### Flutter CR-16 go_router upgrade — 2026-07-18

Клиент обновил `go_router` `13.2.5 → 17.3.0`; backend API, auth contract и
production deployment не менялись. Router regression tests покрывают auth/public
redirects и nested course paths.

### Flutter Android SAF PDF picker smoke — 2026-07-18

В клиент добавлен отдельный operator-assisted device test настоящего Android
DocumentsUI/file picker. После валидации выбранного fixture используется offline
fake import service; backend endpoints, Firebase Auth, Redis/cache contract и
production deployment не менялись и тестом не вызываются. Gate прошёл 1/1 на
Samsung SM-G985F, production package был сохранён, временные fixture и integration
package удалены.

Запуск: `tool/run_android_saf_picker_smoke.sh <device-id>`, затем оператор вручную
выбирает `Downloads/ExamTrainerSafFixture/exam-trainer-saf-valid.pdf`. Это действие
не автоматизируется координатами: внешний DocumentsUI зависит от OEM и локали.

### Flutter CR-13 exercise lifecycle follow-up — 2026-07-18

Шесть exercise-экранов получили общий generation/dispose guard для повторного
использования route с новым course/index. Backend API, course/cache contract и
production deployment не менялись; добавлена клиентская widget-регрессия на
устаревший delayed loader.

### Flutter CR-07 UID switch hardening — 2026-07-18

Клиентский `CourseStorage.loadAll()` больше не возвращает уже прочитанные курсы
прежнего UID, если Firebase session меняется во время local/remote await или
error fallback. Backend API, Firestore schema, outbox и deployment не менялись.

Межустройственный delete-vs-stale-upload остаётся открытым продуктовым
решением. Предварительно подтверждён минимальный совместимый путь: additive
sync metadata, server revision, persistent tombstone и 409 для stale write;
реализацию не начинать до фиксации conflict UX/policy.

### CR-07 delete-wins course sync — 2026-07-18

Политика подтверждена: удаление побеждает любую старую offline-загрузку, а
повторный импорт создаёт новый UUID. `courses/{uid}/items/{courseId}` теперь
содержит server `revision`, `deleted`, `updatedAt`; legacy active document без
metadata трактуется как revision 0. POST принимает additive
`expectedRevision`, применяет настоящий Firestore REST CAS и возвращает 409 при
конфликте. DELETE с CAS создаёт permanent tombstone, удаляет поле `json` и
идемпотентен; stale/legacy POST tombstone не воскрешает.

GET сохраняет `{courses:[...]}` и добавляет `sync:[{id,revision,deleted,
updatedAt}]`; tombstones не попадают в `courses`. Firestore list failure теперь
503, не fake-empty 200. Course IDs ограничены безопасным форматом, а исторический
JSON выдаётся только при совпадении `course.id` с Firestore document id. CORS
явно разрешает DELETE, невалидные revision/body дают 400 без raw exception.

Flutter-клиент совместимо хранит metadata отдельно от course JSON, поэтому
production cache/backend course format, Free/Premium и Firebase Auth не
менялись. Backend deployment в рамках реализации не выполнялся; перед rollout
сначала запушить оба совместимых commit, затем отдельно deploy и проверить GET,
POST/DELETE conflict и старый клиент на production/staging URL.

Локальная проверка реализации: backend 175/175 + `py_compile`, Flutter 351/351,
coverage 55.08%, clean production APK собран. Device smoke не запускался,
поскольку ADB не видел подключённого устройства. Production deployment всё ещё
не выполнен и требует отдельного разрешения/проверки.

### CR-07 production rollout — 2026-07-18

Commit `2b553e6` развёрнут отдельной production-сборкой Vercel
`dpl_24YhsW6Qewyu3d6AHpDWQL29AWBV` и aliased на
`https://exam-trainer-api.vercel.app`; deployment имеет target Production,
status Ready и видит все требуемые encrypted Firebase/Upstash/Gemini variables.
Safe smoke: OPTIONS `/api/courses` и `/api/convert` — 200, CORS содержит DELETE;
неавторизованные GET `/api/courses`, DELETE `/api/courses/smoke-course` и GET
`/api/me` — безопасные 401 JSON.

Перед правильной production-сборкой preview без Preview environment variables
был кратко promoted, но сразу заменён fresh `vercel deploy --prod` из чистого
HEAD; пользовательские endpoints вызывались только без авторизации, поэтому
Firestore/Redis/user data не изменялись. Authenticated POST→DELETE→stale POST
device smoke ещё требуется через обновлённый клиент или выделенный test account;
не использовать реальные пользовательские курсы как одноразовый fixture.

### CR-07 two-device production smoke — 2026-07-19

Production auth/sync проверен на SM-S938B и SM-G985F с одним Firebase account.
При offline G985F курс удалён на S938B; после reconnect remote tombstone удалил
на G985F только старый совпавший UUID, а S938B после reload остался пустым.
Воскрешения удалённого server id не произошло. Повторный импорт того же PDF
прошёл через cache примерно за 15 секунд, создал новый UUID и синхронизировался
на оба телефона.

Physical gate подтверждает deployed Firestore tombstone/read/write path и
re-import policy. Узкий stale pending POST→409 race остаётся доказан backend и
Flutter host regressions: release app не содержит debug hook для безопасной
паузы между local save и немедленным upload. Не повторять destructive smoke на
пользовательском курсе без нового дефекта или отдельного disposable account.

### Premium full-PDF gate production test — 2026-07-19

Production-проверка "Новый неизвестный PDF" из таблицы раздела 6 (строка
"Да, до 5 новых документов/день" для Premium). Устройство: Samsung SM-S938B
(USB, RFCY51N8PEK), production APK versionCode 10, Premium badge подтверждён,
библиотека курсов изначально пустая.

Исходный 207-страничный flagship PDF (SHA-256
`53634b0c2c85cb2d6b9d5efabcf54a9a344ce5a7082e7a3b4cd1d0a5926149e9`) дал
production cache-hit и полный курс 12 типов/142 items — ожидаемый путь для уже
известного документа (первая строка таблицы). Чтобы проверить именно вторую
строку таблицы (неизвестный PDF под Premium), к файлу временно добавлена
нейтральная marker-страница: 208 страниц, SHA-256
`77d38b429e996ae85ac30ee60a671fc812d626170b8fcef3fa34bd6ad333a7fc`. Backend
логи подтвердили doc miss и discover miss при `tariff=premium` — 208-страничная
версия прошла полный discovery+parse путь, что и требовалось для проверки
Premium-only ветки, а не была отдана из кэша.

Discovery: 1 call, prompt 226009, candidates 10404, оценка стоимости $0.43265.
Parse: 100 успешных usage calls, prompt 275695, candidates 177993, thoughts
90709, оценка стоимости $0.47198. Итоговая оценка — $0.90463. Во время import
произошёл один HTTP 502 на `/api/parse`; клиентский retry восстановился,
импорт успешно завершился примерно за 5 минут.

Live-счётчики по 12 типам в каноническом порядке: 12,18,11,11,14,12,11,9,15,12,
10,8 = 143. Curated-счётчики по тем же 12 типам: 12,16,13,12,14,12,12,9,13,12,9,8
= 142. Расхождение live vs curated присутствует и не объяснено в рамках этой
сессии.

Все 12 first variants открылись в production UI в content state. Force-stop и
cold launch не потеряли ни один из двух курсов (207- и 208-страничная версии).
Временные PDF удалены с тестового телефона, импортированные курсы оставлены в
библиотеке.

Ограничение сессии: sensitive env переменные Vercel недоступны из CLI, поэтому
безопасно выгрузить live JSON для answer-key/verbatim audit не удалось;
debug/admin backdoor для такой выгрузки не добавлялся.

**Вывод:** structural Premium E2E (Premium-only discovery+parse путь,
`tariff=premium`, 502-retry, persist через force-stop) — PASS. Semantic
correctness — NOT CLOSED: расхождение live (143) vs curated (142) по count —
release risk для раздела 6. Следующая задача — получить live JSON безопасным
способом (без backdoor) и прогнать DTO/answer-key/verbatim/diff audits для
208-страничной версии, прежде чем считать full-PDF gate из раздела 6 полностью
верифицированным в production.

### Premium semantic remediation v38 — код готов, rollout ожидает новый кэш

Три ошибки answer key воспроизведены по сохранённому fixture. Backend добавляет
provenance `[[PDF_CORRECT:...]]` из физической жёлтой подсветки PDF и исправляет
ответ только при уникальном option text в parse response. Для Sprachbausteine
Teil 2 есть отдельный fallback по явному inline-ключу `N (a - text)`: только для
этого section type, при единогласии edition chunks и exact letter/text. Любой
конфликт означает no-op. На fixture исправлены Beschwerde v5 Q19,
Sprachbausteine Teil 2 v1 Q55 и Hören Teil 3 v5 Q34. Одинаковый option text в
разных вопросах больше не может вызвать детерминированную mutation.

Rollout обратно совместим: `/api/convert` и `/api/parse` включают v38-семантику
только при точном заголовке `X-Exam-Trainer-Answer-Markers: v38`; без него
conversion/parse остаются legacy-v37. Flutter v38 отправляет заголовок на обоих
convert upload paths и parse. CORS разрешает заголовок. `inject_curated.py`
больше не имеет v36→v37 defaults: source/target marker formats и версии
задаются явно, а Redis-ключи вычисляются по двум независимым digest.
Legacy converter сверён с сохранённым production `source.md`: exact match,
522901 chars, SHA-256 `fc18d533fbef810bcaaef9bd65241b03895d50e5beabee1be6b2393682a3a86f`.

Offline semantic gate работает без сети и fail-closed отклоняет empty course,
missing/duplicate identity и payload drift. Metadata-флаг не разрешает менять
`audio_url`/`pair_audio_url`. Реальный live-vs-curated результат ожидаемо FAIL:
21 exact, 8 metadata-only, 86 payload-changed, 28 fresh-only, 27 trusted-only.
Live 143 JSON нельзя публиковать/сливать. Curated updater также отклоняет
malformed identity до сравнения.

Безопасный порядок: deploy dual-format backend; доказать прежний legacy digest;
вычислить v38 markdown/digest; dry-run и read-back проверенного curated 142-item
course только в `v30.v38|doc|<new-digest>`; прогнать offline/checklist gate;
лишь затем build/install v38 APK и Premium/Free cache smoke. Сейчас Redis,
production backend и APK не изменены; paid Gemini reparse не запускался.

Финальный локальный gate: backend 217 tests + 54 subtests, Flutter 352/352,
coverage 55.25%, format/analyze/compileall/diff-check clean. Android integration
на SM-S938B: PDF smoke 1/1 и Sprachbausteine accessibility 1/1; integration
package удалён, установленный production package сохранён. Clean-built release
APK SHA-256 `11e684a949942f9747e48314f6b793d263b8177eb9e1a7eebb6cfd73f00c7153`,
но его не устанавливать/публиковать до v38 curated cache и versionCode bump.

Dual-format backend commit `ad87d22` развёрнут в production: Vercel deployment
`dpl_Da6iCoFcXQResqAM7tPoDdmdEvbE`, alias
`https://exam-trainer-api.vercel.app`, состояние READY. Safe smoke: `/api/me`
без auth вернул 401 с нормализованным JSON; OPTIONS `/api/convert` вернул 200 и
CORS разрешает новый header. Старый APK продолжает legacy-v37 путь. Redis v38
не опубликован, поэтому Flutter commit `d725b51` только запушен, но APK не
установлен и не распространялся.

### Premium full-PDF semantic audit — 2026-07-19

Follow-up закрыт технически, но выявил semantic failure. Два test-course JSON
без backend backdoor извлечены из app-private storage временной локальной
same-package/same-release-cert diagnostic сборкой; production APK сразу
восстановлен и оба курса сохранились. Артефакты лежат вне репозиториев в
`/home/igor/Downloads/exam-trainer-audit-live/`; production Redis/Firestore и
backend deployment не изменялись.

Flutter DTO gate прошёл для cached 142 + live 143 items (все 12 типов).
Structural diff live против curated v37: 21 exact, 122 new/changed; среди общих
identity 9 metadata/audio-only и 85 payload-changed, плюс 28 live-only и 27
curated-only. Это содержательная нестабильность cold parse, не только voice
metadata или один лишний item.

PDF-highlight gate: 228 OK, 15 NO_MARK, 3 реальные MISMATCH, 1 false-positive
AMBIGUOUS. Подтверждённые ошибки: Beschwerde v5 Q19 (`c` вместо `a`),
Sprachbausteine Teil 2 v1 Q55 (`c` вместо `a`), Hören Teil 3 v5 Q34 (`c` вместо
`a`). Все 24 verbatim candidates независимо сверены и признаны допустимыми
slash/edition/synthetic-x/gap-placeholder representations; по ним данные не
патчить. Сам checker нуждается в edition-aware exemptions, чтобы не возвращать
ложный nonzero.

Решение: curated v37 остаётся production canonical/cache; 143-item live JSON
нельзя inject/merge/publish. Строка Premium «новый неизвестный PDF» проходит
transport/structural E2E, но semantic gate остаётся FAIL. Следующая фаза должна
исправить version identity/discovery drift и answer-key fidelity, иметь offline
regression на сохранённом live fixture и запускать новый paid reparse только
после зелёного детерминированного gate.

### Production rollout v38 — 2026-07-20

Dual-format backend остаётся deployment
`dpl_Da6iCoFcXQResqAM7tPoDdmdEvbE`. Проверенный 142-item curated Redis value
перенесён без нового Gemini parse из legacy `v30.v37` doc-key в вычисленный
marker-aware `v30.v38` doc-key. Dry-run подтвердил source HIT/target MISS;
apply и отдельный read-back подтвердили точное байтовое равенство, SHA-256
`adf1cbca8b386eea6d62215fc6df0c158b6928060e800ea952f887768e4c8870`, 12
sections/142 items. Source v37 не изменён. Temporary Upstash env уничтожен
после проверки; credentials нигде не сохранены.

Flutter production build поднят до versionCode 11, clean APK/AAB собраны и
проверены (package, production API, upload certificate, hashes). SM-G985F
обновлён без очистки данных. Backend regression: 217/217 + compileall. Flutter:
352/352, coverage 55,26%, format/analyze clean. Остаточный rollout gate —
Premium/Free physical-device cache smoke. Wireless Flutter integration дважды
оборвался на test-harness WebSocket до test body; production package versionCode
11 сохранился, integration package удалён. До зелёного device-smoke AAB не
публиковать и новый paid parse не запускать.

После смены wireless-debug порта SM-G985F прошёл PDF integration 1/1 и
Sprachbausteine accessibility 1/1. Production cold launch завершил preload,
два курса сохранились, профиль подтвердил Free, разделы cached course доступны
ровно по одной вариации. Free/device gate закрыт. До публикации AAB остаётся
Premium v38 cache-hit исходного 207-page PDF на SM-S938B; unknown PDF и paid
parse для этого не нужны.

Premium gate завершён: SM-S938B обновлён до production versionCode 11 без
очистки данных, exact flagship PDF (SHA-256 `53634b0c...`) импортирован примерно
за 26 секунд и создал третий 142-item course. Production log подтвердил
`GET /api/cache` по `v30.v38|doc|d4103bd6...`, `CACHE_LOOKUP hit=True`, HTTP
200; Gemini parse не запускался. Временный PDF удалён с устройства. Free и
Premium rollout v38 теперь закрыт, verified AAB можно передавать в ручной Play
Console workflow.

### Первый проход backend security review (частично) — 2026-07-20

v38 rollout полностью закрыт (см. выше); следующий незакрытый пункт взят из
P1 Этап 2 CODE_REVIEW_2026-07-15.md, item 6 «Провести отдельный backend
security review» — этот пункт ни разу не был отмечен выполненным ни в одном
более позднем статусе. Полностью прочитаны `main.py`, `firebase_auth.py`,
`firestore_client.py` (auth verification, UID isolation, device gate, course
sync, account deletion). Firestore security rules не применимы — Flutter не
использует `cloud_firestore` напрямую, весь доступ идёт через backend с
Admin-эквивалентным service-account REST, поэтому «Firestore rules audit» из
исходной формулировки не имеет смысла в этой архитектуре; сама архитектура
(сервер как единственная точка доступа к Firestore) — это фактически
правильный периметр.

Найдены и исправлены два реальных дефекта, оба verified через код + новые
regression-тесты (CodeGraph `impact`/`callers` подтвердили: у обоих
изменённых символов ровно один call site, blast radius не выходит за пределы
`main.py`):

1. **Cache poisoning в `POST /api/cache`.** Эндпойнт принимал `{hash, value}`
   от ЛЮБОГО аутентифицированного пользователя (free тоже — эндпойнт не
   премиум-gated) и безусловно перезаписывал существующий ключ, включая
   единственный вручную выверенный `doc`-ключ `v30.v38|doc|...`, на который
   опирается подавляющее большинство пользователей (см. Фазу 1/3 — реальные
   деньги и ручная сверка ушли именно в этот ключ). Никакой связи между
   присланным `hash` и `value` сервер не проверял. `tools/inject_curated.py`
   уже реализует «никогда не перезаписывать другой value» для прямого
   доступа к Redis (`migrate()`); тот же инвариант теперь применяется и к
   публичному эндпойнту — write-once (перезапись разрешена только если
   новое значение байт-в-байт совпадает со старым), плюс cap 4 MiB на
   размер value. Публикация курируемого контента не затронута:
   `inject_curated.py` обращается к Upstash напрямую своими credentials, а
   не через этот роут. 8 новых regression-тестов в
   `tests/test_cache_endpoint_contract.py` (эндпойнт раньше не имел ни
   одного теста).
2. **Непровалидированный `deviceId` в `/api/device` и `/api/device/force`.**
   `device_id` конкатенировался прямо в Firestore REST путь
   (`users/{uid}/devices/{deviceId}`) без какой-либо проверки формата — в
   отличие от `course_id`, для которого точно такая же защита
   (`_valid_course_id`/`_COURSE_ID_RE`) уже существовала именно по этой
   причине (см. комментарий у `courses()`). Значение с `/`- или
   `..`-сегментами теоретически может адресовать документ вне `users/{uid}`
   через нормализацию dot-segment на HTTP-уровне — не подтверждено живым
   вызовом (Firestore не трогали), но защита добавлена как defense-in-depth
   по тому же принципу, что уже применён к `course_id`. Реальный `deviceId`
   — всегда `Uuid().v4()` (`DeviceService.getDeviceId` во Flutter), поэтому
   новый `_valid_device_id`/`_DEVICE_ID_RE` (тот же charset, что и у
   course id) не меняет поведение ни для одного легитимного клиента. 4
   новых теста в `tests/test_device_endpoint_contract.py`.

Backend gate: 229/229 (было 217, +8 cache +4 device), `py_compile` чист,
`git diff --check` чист. Flutter не менялся (найденные и исправленные
проблемы — исключительно backend), Flutter gate для этой сессии не
перезапускался. Изменения не задеплоены — как и весь остальной P1/P2 backend
код, дальнейший `vercel deploy` требует отдельного подтверждения
пользователя.

**Это НЕ полный security review, только первый проход.** Не проверялись
глубоко: устойчивость `_rate_limit_ok`/`_incr_with_ttl` к распределённому
злоупотреблению за пределами уже реализованной Фазы 0, обработка ошибок в
`tts.py`, потенциальный prompt injection через содержимое PDF в
Gemini-промпты (`prompts.py`), ротация/утечка Upstash и Gemini API-ключей,
устойчивость `response_schemas.py`/`span_resolution.py` к откровенно
враждебному markdown. Пункт P1 «backend security review» остаётся частично
открытым — эти два найденных дефекта были самыми конкретными и проверяемыми
за один проход, не исчерпывающим списком.

### CR-17 доработан: write-once оказался недостаточным — 2026-07-20

**Важно: пункт «Cache poisoning в `POST /api/cache`» выше (закрыт commit
`b9345b2`) был закрыт преждевременно.** Независимая перепроверка того же дня
нашла, что write-once реализация (раздельные `_cache_get()` → `_cache_set()`)
неатомарна и остаётся first-writer poisoning. Это НЕ отменяет находку самого
CR-17 — исходная проблема (любой authenticated пользователь может перезаписать
существующий cache-ключ) была реальной и правильно найдена; недостаточной
оказалась ТОЛЬКО конкретная реализация фикса. Оставляю оригинальный текст
выше без изменений (только эта секция добавлена) — история находки и
первого, неполного фикса пусть останется видна.

Подтверждённые независимой перепроверкой проблемы:

1. **Неатомарность.** Два параллельных запроса к одному ещё отсутствующему
   ключу оба видят MISS и оба выполняют SET; воспроизведено детерминированно
   (barrier-synchronized concurrency harness, 100% повторяемо за 5 прогонов).
2. **First-writer poisoning остаётся.** Любой authenticated пользователь
   (free тоже, premium тоже — оплата premium ничего не доказывает) мог быть
   первым писателем ещё отсутствующего shared-ключа и создать его с
   произвольным содержимым — просто атомарный `SET NX` этого не закрывает,
   потому что проблема не в атомарности самого SET, а в том, что аутентификация
   ничего не говорит о ПРАВИЛЬНОСТИ значения.
3. Cache key не валидировался (произвольные path/query-like фрагменты могли
   попасть в исходящий Upstash REST URL).
4. Malformed non-object JSON (`[]`, `"str"`, `42`, `null`) вызывал
   `AttributeError` → сырой 500 на `POST /api/cache`, `/api/device`,
   `/api/device/force`.
5. Конфликт (отличающееся значение) маскировался под `200 {"ok": true}` —
   ни клиент, ни логи не видели разницы между «принято» и «отклонено».
6. Ошибка/недоступность Upstash не отличалась от «ключ подтверждённо
   отсутствует» — при сбое Redis код мог трактовать неопределённое состояние
   как основание для действия.

**Выбранная модель доверия для cache-записи — переписана полностью, не
пропатчена:** `POST /api/cache` теперь НИКОГДА не создаёт новый shared-cache
ключ. Единственная Redis-операция, которую выполняет этот handler —
`_cache_probe()` (чтение); никакого `SET`/`_cache_set` в этом коде больше нет
вообще (`_cache_set` удалена из `main.py` полностью, не оставлена
неиспользуемой). Три исхода:

- существующее значение совпадает байт-в-байт → `200 {"ok": true}`
  (идемпотентно, без записи);
- существующее значение отличается → `409 {"ok": false, "conflict": true}`
  (без записи, наблюдаемо в логах как `CACHE_WRITE_REJECTED_CONFLICT`);
- ключ ещё не существует → `403` (создание отклонено, НЕ гонка за право
  создать — создавать нельзя вообще; в логах `CACHE_WRITE_REFUSED_NO_CREATE`);
- Redis недоступен/вернул неожиданный ответ → `503`, никогда не `200`
  (новое исключение `_CacheUnavailable`, отдельное от «подтверждённо
  отсутствует», которое даёт `_cache_probe`).

**Почему first-writer poisoning теперь структурно невозможен, а не просто
менее вероятен:** убрана сама возможность записи из этого эндпойнта — нет
`SET`-вызова, значит нет и «кто первый — тот и прав». Атомарный `SET NX`
закрыл бы только гонку (проблему 1), но не проблему 2 — при `SET NX`
оставался бы легитимный, атомарный способ для ЛЮБОГО authenticated клиента
стать первым и единственным писателем произвольного значения. Оба паттерна
устранены одним архитектурным решением, а не двумя отдельными патчами.

**Как обеспечена атомарность:** отсутствием операции записи, а не атомарной
версией записи. Конкурентность у `cache_endpoint` теперь состоит из
независимых чтений одного и того же уже существующего (или отсутствующего)
состояния — параллельным запросам нечего гонять, потому что ни один из них
ничего не пишет.

**Публикация нового shared-контента:** только вне этого эндпойнта, через
`tools/inject_curated.py` — тот же env `UPSTASH_REDIS_REST_URL`/`_TOKEN`
(не отдельный секрет), но отдельный, вручную запускаемый CLI-путь, который
обходит Flask endpoint и Firebase auth целиком, требует human-reviewed
checklist (Фаза 3) и уже сам не даёт перезаписать другой value (`migrate()`).
**Побочный эффект, требующий отдельного продуктового решения:**
`inject_curated.py` публикует только `doc`-ключи; `group`/`discover`-кэш
(который раньше писал ЛЮБОЙ клиент любого тарифа при живом импорте — там не
было premium-gate даже до этого фикса) теперь не может создать никто.
Каждый group/discover lookup для контента, которого ещё нет в общем кэше,
будет промахиваться и заново идти в Gemini — реальная деградация cost/latency
для незнакомых документов, не только косметика. Рекомендованный, но НЕ
реализованный в этой сессии follow-up: доверенная server-side запись прямо
из `/api/parse` (сервер сам решает, что писать, на основе данных, которые
только что получил от Gemini — не нужно доверять произвольному клиентскому
POST). Это отдельное архитектурное решение с более широким blast radius,
затрагивающее `/api/parse`, сознательно не сделано в рамках этого fix.

**Контракт публичной записи изменился, но обратно совместимо.** Flutter
`ParseService._cacheSet` (`lib/services/parse_service.dart:255`) отправляет
POST и полностью игнорирует статус/тело ответа (`try { await http.post(...) }
catch (_) {}` — переменная результата даже не сохраняется). Новые коды 403/
409/503 невидимы клиенту; `_cacheGet` уже трактовал любой не-200 как miss.
Формат самого ключа (`<version>|<type>|<hash>` или legacy bare-hash)
НЕ изменился — только то, что сервер теперь делает с ним при записи.

Реализация и валидация: главный агент спроектировал и написал fix
(`main.py`: `_cache_probe`, `_CacheUnavailable`, `_valid_cache_key`,
`_json_object`, переписанный `cache_endpoint`; то же `_json_object`
дополнительно закрыло malformed-JSON 500 на `/api/parse` и `/api/tts` —
тот же класс бага, вне исходно перечисленных 3 эндпоинтов, но тривиально
безопасное расширение). Три субагента использованы для независимых
направлений без одновременного редактирования одних файлов: (1) threat-model
аудит всех callers `/api/cache` в обоих репозиториях (Sonnet) — подтвердил
полную обратную совместимость и указал, что «никогда не создавать» отключает
запись для ВСЕХ трёх типов ключей, не только `doc`; (2) проектирование и
реализация concurrency-regression тестов (Sonnet) — написал
`tests/test_cache_concurrency_regression.py` с barrier-synchronized
threading и отдельным доказательством, что тот же harness ловит СТАРЫЙ
баг (воспроизведение GET-then-SET паттерна локально, вне Flask, показывает
гонку на 6/6 потоках); (3) независимая скептическая проверка финального
диффа (Opus) — подтвердила все 6 пунктов CLOSED, самостоятельно прогнала
тесты, попробовала обойти `_valid_cache_key` `/`, `..`, `%2e%2e`, control
characters, кириллические homoglyph-и — всё отклонено; нашла две
формулировочные неточности (не баги — "separate credentials" неточно описан
как отдельный секрет, group/discover cost regression недостаточно
подчёркнута), обе исправлены в коде и в этом разделе.

Regression-тесты, покрывающие все обязательные сценарии из независимой
перепроверки: два разных значения параллельно на отсутствующий ключ (никто
не побеждает — оба отклонены), идемпотентность при совпадающем существующем
значении, conflict при отличающемся, невозможность перезаписать curated
`doc`-ключ, невалидный/длинный/path-like ключ → 400, malformed JSON на всех
5 эндпоинтах → 400, oversized value отклоняется до обращения к Redis,
недоступность Upstash не маскируется под success, и доказательство, что
concurrency harness ловит именно старый баг (не тавтологичен).

Gate: backend **244/244** (было 229), `tests/test_cache_concurrency_regression.py`
стабильно 4/4 на 5 повторных прогонах подряд, `py_compile` чист по всем
`.py` файлам репозитория, `git diff --check` чист, поиск секретов в diff —
чисто. Flutter не менялся (контракт обратно совместим, подтверждено
дважды независимо — subagent 1 и subagent 3), Flutter gate не
перезапускался.

**CR-17 теперь можно считать закрытым** — обе подтверждённые проблемы
(неатомарность и first-writer poisoning) устранены одним структурным
решением, независимо перепроверено скептическим отдельным проходом (Opus),
который явно пытался найти обходной путь и не нашёл. Deployment в Vercel
по-прежнему НЕ выполнен и не разрешён без отдельного явного подтверждения
пользователя — этот фикс, как и весь остальной backend security review,
существует только в git, ожидая осознанного решения о деплое.
